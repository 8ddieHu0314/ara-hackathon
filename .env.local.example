# URL of the Python FastAPI backend. Defaults to http://localhost:8000 if unset.
# All Ara credentials and the persistence store now live in the backend —
# see backend/.env.example for those.
BACKEND_URL=http://localhost:8000
